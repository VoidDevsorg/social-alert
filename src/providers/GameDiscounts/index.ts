import { EventEmitter } from 'node:events';

export class Instagram extends EventEmitter {
    constructor() {
        super();

        console.log('Instagram is not yet implemented.');
    }
}