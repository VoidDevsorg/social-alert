import { EventEmitter } from 'node:events';

export class GameDiscounts extends EventEmitter {
    constructor() {
        super();

        console.log('GameDiscounts is not yet implemented.');
    }
}